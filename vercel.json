{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!assets|favicon.ico|manifest.json|robots.txt|placeholder.svg).*)", "destination": "/index.html" }
  ]
}
